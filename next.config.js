/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

const nextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com"]
  }
};

module.exports = nextConfig;

